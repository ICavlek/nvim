# nvim
Neovim configurations

# Setup

Clone the repository in:

```shell
~/.config/
```

When asked to use the default configs on NVChad, press N and wait for it to install all necessary things.
Run the following commands to startup the application:

```shell
:MasonInstallAll
:TSInstall vim
:TSInstall vimdoc
:TSInstall rust
```
